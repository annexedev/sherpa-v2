import React from 'react';

import LoadingIndicator from './indicator';

const staticIndicator = (
    <LoadingIndicator global={true}>{'Fetching'}</LoadingIndicator>
);

export default staticIndicator;
