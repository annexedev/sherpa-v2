query navigationMenu($id: Int!) {
    category(id: $id) {
        name
        id
        children {
            children_count
            id
            include_in_menu
            name
            position
            url_path
            url_suffix
        }
    }
}
